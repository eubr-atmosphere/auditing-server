# Required
deployed_host_ip=10.11.16.40

# Required
remote_user=ubuntu

# Required (if not specified, ansible will use the host ssh keys)
ansible_ssh_private_key_file=/home/raonismaneoto/Downloads/id_rsa_fogbow_default_private_key

# Standard
ansible_files_path=./ansible-playbook
